#include <iostream>
#include<string.h>
using namespace std;

int main()
{ char ch1[100],ch2[100];
int l1,l2,i=0,j=0;
do
{
    cin>>ch1>>ch2;
    l1=strlen(ch1);
    l2=strlen(ch2);
    while(l1>=&&j>=l2)
     if(ch1[i]>ch2[l2])
    temp=1;

}
while(ch!='*'||ch!='*') ;

return 0;
}
